services:
  backend:
    platform: linux/x86_64
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "80:80"
    volumes:
      - ./backend/src/:/www/
      - ./ecogroup/:/www/ecogroup

volumes:
  backend:
