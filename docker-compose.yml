version: '3.9'

services:
  fullstack:
    image: tomsik68/xampp
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "3000:3000"
    volumes:
      - ./backend/src:/www/
      - ./ecogroup/dist/:/www/dist
